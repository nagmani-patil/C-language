#include <stdio.h>

float findAverage(int arr[], int n)
{
    int sum = 0;

    for(int i = 0; i < n; i++)
    {
        sum += arr[i];
    }

    return (float)sum / n;
}

int main()
{
    int arr[5] = {10, 20, 30, 40, 50};

    printf("Average = %.2f", findAverage(arr, 5));

    return 0;
}