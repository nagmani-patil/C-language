#include <stdio.h>

int main()
{
    char str[100];
    int i;

    printf("Enter a string: ");
    scanf("%99s", str);

    for(i = 0; str[i] != '\0'; i++)
    {
        if(str[i] == 'a')
            str[i] = '$';
    }

    printf("Result: %s", str);

    return 0;
}