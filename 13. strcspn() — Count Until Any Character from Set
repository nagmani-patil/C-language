#include <stdio.h>
#include <string.h>

int main()
{
    char str[] = "Hello, World!";
    char set[] = ",!";

    size_t result = strcspn(str, set);

    printf("Characters before delimiter = %zu\n", result);

    return 0;
}