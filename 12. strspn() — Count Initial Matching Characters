#include <stdio.h>
#include <string.h>

int main()
{
    char str[] = "12345ABC";
    char set[] = "1234567890";

    size_t result = strspn(str, set);

    printf("Matching characters = %zu\n", result);

    return 0;
}